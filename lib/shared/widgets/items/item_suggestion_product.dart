import 'package:flutter/widgets.dart';

class ItemSuggestionProduct extends StatelessWidget {
  const ItemSuggestionProduct({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [],
    );
  }
}
